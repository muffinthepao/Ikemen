<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <script src="https://kit.fontawesome.com/3faa00ba1d.js" crossorigin="anonymous"></script>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            clifford: '#da373d',
          }
        }
      },
      plugins: [
        require('@tailwindcss/typography'),
        require('@tailwindcss/forms'),
        require('@tailwindcss/line-clamp'),
        require('@tailwindcss/aspect-ratio'),
      ]
    }
  </script>

  <link rel="stylesheet" type="text/css" href="/style.css" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
  <link rel="stylesheet" href="https://rsms.me/inter/inter.css">
  <link rel="stylesheet" href="https://unpkg.com/flowbite@1.5.1/dist/flowbite.min.css" />
  <link href="https://api.mapbox.com/mapbox-gl-js/v2.9.2/mapbox-gl.css" rel="stylesheet">
  <script src="https://api.mapbox.com/mapbox-gl-js/v2.9.2/mapbox-gl.js"></script>

  <title>Ikemen | For Noodle Lovers</title>
</head>
<body class="flex flex-col h-screen justify-between">
  <header class="text-gray-600 body-font">
    <div class="container mx-auto flex flex-wrap p-5 flex-col md:flex-row items-center">
      <a href="/" class="flex title-font font-medium items-center text-gray-900 mb-4 md:mb-0">

        <span class="material-symbols-outlined w-10 h-10 text-white p-2 bg-red-500 rounded-full">
          ramen_dining
        </span>

        <span class="ml-3 text-xl">Ikemen</span>
      </a>
      <nav class="md:mr-auto md:ml-4 md:py-1 md:pl-4 md:border-l md:border-gray-400	flex flex-wrap items-center text-base justify-center">
        <a href="/" class="mr-5 hover:text-red-500">Home</a>
        <a href="/food?page=1" class="mr-5 hover:text-red-500">Food</a>
        <a href="/surprise" class="mr-5 hover:text-red-500"> Surprise Me!</a>
      </nav>
            
      <% if (authUser) { %>
        <button class="inline-flex items-center bg-red-100 border-0 py-1 px-3 focus:outline-none hover:bg-red-400 hover:text-white rounded text-base mt-4 md:mt-0">
          <a href="/user/profile"><%= authUser %></a>
        </button>

        <form class="inline-flex items-center border-0 py-1 px-3 focus:outline-none hover:text-red-500 rounded text-base mt-4 md:mt-0" method="POST" action="/logout">
          <button>Logout</button>
        </form>
  
      <% } else { %>
        <button class="inline-flex items-center border-0 py-1 px-3 focus:outline-none hover:text-red-500 rounded text-base mt-4 md:mt-0">
          <a href="/login">Login</a>
        </button>
  
        <button class="inline-flex items-center bg-red-100 border-0 py-1 px-3 focus:outline-none hover:bg-red-400 hover:text-white rounded text-base mt-4 md:mt-0">
          <a href="/register">Register</a>
        </button>
      <% } %>
    </div>
  </header>
  <header>
  </header>

  <main>